import { Usuarios } from '../models/AdminUsers/Usuarios.model';

export default defineNuxtRouteMiddleware(async(to, from) => {
    const router = useRouter(); //* redirecciones

    // if(to.path == '/'){
    //     router.push('/nuevaAsesoria')
    // }

});
